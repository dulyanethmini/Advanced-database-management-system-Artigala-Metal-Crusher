@page

@using System.Data.SqlClient;
@{

    try
    {
        String Material_id = Request.Query["Material_id"];

        String connectionString = "Data Source=.\\sqlexpress;Initial Catalog=rockgroup;Integrated Security=True";
        using (SqlConnection connection = new SqlConnection(connectionString))
        {
            connection.Open();
            String sql = "DELETE FROM Material WHERE Material_id=@Material_id";
            using (SqlCommand command = new SqlCommand(sql, connection))
            {
                command.Parameters.AddWithValue("@Material_id", Material_id);
                command.ExecuteNonQuery();

            }
        }
    }

    catch (Exception ex)
    {
        
    }
}

