@page
@model Project.Pages.SizeClassification.CreateModel
@{
    ViewData["Title"] = "New Size Classification";
}

<br>
<h2>New Size Classification</h2>

@if (!string.IsNullOrEmpty(Model.ErrorMessage))
{
    <div class="alert alert-warning alert-dismissible fade show" role="alert">
        <strong>@Model.ErrorMessage</strong>
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
}

<form method="post">
    <div class="row mb-3">
        <label asp-for="SizeClassification.Size_id" class="col-sm-3 col-form-label">Size ID</label>
        <div class="col-sm-6">
            <input asp-for="SizeClassification.Size_id" class="form-control" />
            <span asp-validation-for="SizeClassification.Size_id" class="text-danger"></span>
        </div>
    </div>
    <div class="row mb-3">
        <label asp-for="SizeClassification.size_description" class="col-sm-3 col-form-label">Size Description</label>
        <div class="col-sm-6">
            <input asp-for="SizeClassification.size_description" class="form-control" />
            <span asp-validation-for="SizeClassification.size_description" class="text-danger"></span>
        </div>
    </div>
    <div class="row mb-3">
        <label asp-for="SizeClassification.Material_id" class="col-sm-3 col-form-label">Material ID</label>
        <div class="col-sm-6">
            <input asp-for="SizeClassification.Material_id" class="form-control" />
            <span asp-validation-for="SizeClassification.Material_id" class="text-danger"></span>
        </div>
    </div>

    @if (!string.IsNullOrEmpty(Model.SuccessMessage))
    {
        <div class="row mb-3">
            <div class="col-sm-9">
                <div class="alert alert-success alert-dismissible fade show" role="alert">
                    <strong>@Model.SuccessMessage</strong>
                    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                </div>
            </div>
        </div>
    }

    <div class="row mb-3">
        <div class="offset-sm-3 col-sm-3 d-grid">
            <button type="submit" class="btn btn-primary">Submit</button>
        </div>
        <div class="col-sm-3 d-grid">
            <a class="btn btn-outline-primary" href="/Material/Index" role="button">Cancel</a>
        </div>
    </div>
</form>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
